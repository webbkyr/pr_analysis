## PR Analysis for Repositories on Github

## running

`npm install`
`npm run build`
`npm start`

## developing

`npm run build:live` for auto refresh (be mindful of rate limiting because the main function with automatically execute)